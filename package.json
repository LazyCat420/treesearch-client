{
  "name": "cannabis-researcher",
  "version": "0.1.0",
  "description": "Unified cannabis data warehouse + analysis service",
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8002 --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8002"
  },
  "deploy": {
    "image": "cannabis-researcher",
    "port": 8002,
    "env": {
      "PORT": "8002",
      "KANNAPEDIA_DATA_DIR": ""
    }
  }
}
